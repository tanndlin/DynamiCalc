import React from 'react';

const Header = () => {
    return (
        <header className="text-4xl font-bold flex text-center bg-sky-900 text-white h-16">
            <h1 className="m-auto">DynamiCalc</h1>
        </header>
    );
};

export default Header;
